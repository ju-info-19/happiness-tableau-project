# Partie 5 – Tâche 1 : Tableau de texte dégradé bleu – pays les moins heureux

1. Feuille `Heatmap Bonheur Bottom 10`.
2. **Lignes** : `Country`, filtré avec le champ calculé `Pays Top Least` (cf. Partie 2 Tâche 3).
3. Exclure **Syria** et **Central African Republic** : glisser `Country` sur Filtres, décocher ces deux pays.
4. **Colonnes** : `Year`.
5. **Texte** et **Couleur** : `AVG(Happiness Score)` (dégradé bleu).
6. Titre : `Score de bonheur par pays et année – Bottom 10`.
7. Option filtre activée.