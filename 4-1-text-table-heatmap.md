# Partie 4 – Tâche 1 : Tableau de texte avec dégradé bleu

1. Feuille `Heatmap Bonheur Top 10`.
2. **Lignes** : `Country` (filtré avec `Pays Top Happy`).
3. **Colonnes** : `Year`.
4. **Texte** : `AVG(Happiness Score)`.
5. **Couleur** : `AVG(Happiness Score)` (dégradé bleu, clair à foncé).
6. Type : **Tableau de texte**.
7. Titre : `Score de bonheur par pays et année – Top 10`.
8. Activez **Utiliser comme filtre** pour le tableau de bord.